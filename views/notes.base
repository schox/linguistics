filters:
  and:
    - 'type == "Note"'
formulas:
  title: file.asLink(aliases[0])
properties:
  formula.title:
    displayName: Title
views:
  - type: table
    name: Notes
    order:
      - formula.title
      - subfield
      - status
    sort:
      - property: status
        direction: ASC
