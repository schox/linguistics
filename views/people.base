filters:
  and:
    - type == "Person"
formulas:
  title: file.asLink(aliases[0])
properties:
  formula.title:
    displayName: Title
views:
  - type: table
    name: People
    order:
      - formula.title
      - field
      - born
      - died
    sort:
      - property: born
        direction: DESC
